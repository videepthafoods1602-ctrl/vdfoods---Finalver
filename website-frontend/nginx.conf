server {
    listen 80;
    server_name videepthamdfoods.com www.videepthamdfoods.com;
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    server_name videepthamdfoods.com www.videepthamdfoods.com;

    ssl_certificate /etc/letsencrypt/live/videepthamdfoods.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/videepthamdfoods.com/privkey.pem;

    # SSL optimizations
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;

    location / {
        root /usr/share/nginx/html;
        index index.html index.htm;
        try_files $uri $uri/ /index.html;
    }

    location /api/ {
        proxy_pass http://backend:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /admin/ {
        proxy_pass http://backend:8000/admin/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Django static files (admin, etc.)
    location /static/admin/ {
        proxy_pass http://backend:8000/static/admin/;
    }
    
    location /static/rest_framework/ {
        proxy_pass http://backend:8000/static/rest_framework/;
    }

    # Media files
    location /media/ {
        alias /app/media/;
    }

    location /.well-known/acme-challenge/ {
        root /var/lib/letsencrypt/;
    }

    error_page 500 502 503 504 /50x.html;
    location = /50x.html {
        root /usr/share/nginx/html;
    }
}
